myApp {

  testval = "happy"

  akka {
    loglevel = "DEBUG"
    log-dead-letters = off
    log-dead-letters-during-shutdown = off

    actor {
      debug {
        unhandled = on
        receive = on
      }
    }
  }
}